app-id: org.flatpak.markdownEdit
runtime: org.kde.Platform
runtime-version: '6.3'
sdk: org.kde.Sdk
command: markdownedit
rename-desktop-file: org.flatpak.markdownedit.desktop
rename-icon: Icon.png
finish-args:
  - --share=ipc
  - --socket=x11
  - --socket=wayland
  - --filesystem=host
  - --device=dri
modules:
  - name: markdownedit
    buildsystem: cmake-ninja
    config-opts:
      - -DFLATPAK=ON
      - -DCMAKE_BUILD_TYPE=RelWithDebInfo
    sources:
      - type: git
        url: https://github.com/software-made-easy/MarkdownEdit.git
        branch: main
finish-args:
  - --filesystem=home:rw

